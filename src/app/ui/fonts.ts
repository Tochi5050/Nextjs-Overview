import {Lusitana} from 'next/font/google'
import {Tilt_Neon} from 'next/font/google'

export const Tilt = Tilt_Neon({weight: '400', subsets: ['latin'], display: 'swap'})

export const lusitana = Lusitana({weight: '400', subsets: ['latin'], display: 'swap'})